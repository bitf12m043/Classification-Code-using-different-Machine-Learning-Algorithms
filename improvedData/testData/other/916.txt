Date Tue Dec GMT Server NCSA Content type text html Recent ZPL Language Changes Recent ZPL Language ChangesThe changes below are effective July The newly installed version ZPL improved two ways multitude small language changes have been incorporated make the language more convenient powerful and many bugs have been fixed The resulting compiler considerably more stable and the language more convenient However MOST EXISTING ZPL PROGRAMS WILL HAVE CHANGE SOMEWHAT Though possibly causing some inconvenience for the moment this one time change ZPL essentially stable with all subsequent research being applied new language The following language feature changes will likely require modifications existing programs Some data type have been introduced eliminated renamed Signed and unsigned byte data types are now supported sbyte and ubyte and unsigned char longer exists The unsigned data types have been renamed summarized below unsigned shortint ushortint unsigned integer uinteger unsigned longint ulongint Exponentiation added The binary operator has been added The expression raises the power The compiler generates direct evaluation when integer constant All other cases compile call pow The functions sqr and cube have been removed Bit operations have been changed band bitwise and integers bor bitwise integers bnot bitwise negation integer bxor bitwise exclusive integers bsl integer left shifted bit positions bsr integer right shifted bit positions Logical operations have been changed and continues correct for logical negation Compound assignments have been removed Double brackets may optionally removed most instances Dynamic region specifiers not involving prepositions not require double brackets Thus but west with require both sets brackets This not yet implemented Rank defined arrays procedure specifications have been replaced with dynamic regions that inherit their dimension information from the call site Thus procedure Example float becomes procedure Example float The inheritance capability available with all region specifications empty dimensions are inherited from the enclosing scopes Reduce scan and flood operators have changed Reduce Scan Flood The heuristic here that the result reduce smaller than the operand the result scan the same size the operand and the result flood larger than the operand Region change removed The region change construct used specify the regions for partial reduce and flood has been removed Thus the left arrow construct longer legal Accordingly the partial reduce and flood operators have changed that both specify region following the operator which describes the operand This region together with the region the statement which describes the result supply the needed region information for these operations Thus for example the partial reduction the region following the operator the region the operand the region reduced The region the statement specifies the result region which since relative the operand region has the second collapsed dimension the elements each row are added and the result stored into the first column Similarly for the flood Replicate row into the region following the operand specifies the array replicated and the region the statement specifies the region the result relative the result region the first dimension collapsed ZPL CSE zpl info washington edu 