Date Wed Jan GMT Server NCSA Content type text html Using UNIXUsing UNIXa brief introductionExecutive Summary This document designed help you get started UNIX assumes that you not completely stupid about files and directories and such will only tell you the very most basic est things Introduction UNIX UNIX operating system likes use short two letter commands for most things When you ask stuff doesn usually prompt you make sure you sure also doesn print much back you unless there error Most UNIX commands take arguments which are specified after the command often with File basics The current directory called The parent directory referred Your home directory can referred using The root directory UNIX simply called Files can specified using slashes between directory names For example can refer file named foo home directory foo Paths that start with are absolute pathnames because they refer specific starting point Other paths are relative and constructed starting from the directory you The example above was absolute and will work matter where contrast foo will back one level from the current directory and look for foo Here are some useful commands pwd prints the working directory you name changes you into the the named directory lists the files the current directory Subdirectories are marked with gives you the long version including the dates the files were created their sizes and plenty other stuff that won important lists all the files directory Filenames which start with are not printed otherwise name lists the contents the named directory name new name copies file new name location name new name moves file new name directory the file will moved that location filename will renamed that name name removes file warning mkdir name makes new directory rmdir name removes directory Looking files The best way look file short firing emacs session use more This will display the file one screenful time Between screens you can hit quit RETURN advance one line SPACE advance the next screen You can also search for text using text RETURN Using the shell The shell the command interpreter that you interact with xterm the mscc machines you using shell called tcsh You can run the previous command using You can run command starting with particular substring using substring You can see list the recent commands you typed using history You can also edit previous commands using the arrow keys scroll through them The MSCC machines seem set such way that this isn completely straightforward Type bind emacs make work intuitively Put this line your cshrc have run automatically for you every login cshrc contains commands that will run whenever new shell started line help UNIX every command has line manual entry which very terse but complete description what does Access these entries typing man command You can also use apropos substring get list commands which contain given substring Telnet rlogin You can get access other UNIX machines remotely using telnet machine rlogin machine For example access your MSCC account from departmental machine you would use rlogin mscc this way you never have leave the comfort Sieg Hall your user name different the MSCC cluster you can use username specify what your name there Printing print text files UNIX use enscript Specify the printer with the flag and use save paper also use get the gaudy graphical look Conclusion Those are the most basic basics This should tell you what you need know get through these assignments For the most part you should working within emacs which has been documented elsewhere Feel free ask questions you have questions about UNIX that aren covered here Shamelessly stolen from cse Winter cse webmaster washington edu Last update 