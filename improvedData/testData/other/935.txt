Date Wed Jan GMT Server NCSA Content type text html CSE Assignment Solution Set Friday January Next About this document CSE Assignment Solution Set Friday January Prove that for all can prove this induction Basis The statement true for because Inductive hypothesis Assume that the statement true for Inductive step From this need show true for induction the statement true for all Using the tree The depth the tree The depth the root The ancestors are and Every node ancestor itself The minimal common ancestor and and and The subtree generated the following The frontier the tree The rank the enumeration ordering This because look the strings length the first strings the enumeration are those that begin with the strings that begin with the first and the last these This gives the following The rank The number strings length that begin with just the number ways writing the last letters string the alphabet There are such ending strings Combining these observations gives The rank the enumeration ordering the enumeration ordering the strings that come before are exactly the strings over whose length less than Therefore For any the number strings length Thus the number strings length less than the sum all these About this document Next About this document James Fix Mon Jan PST 