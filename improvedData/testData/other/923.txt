Date Wed Jan GMT Server NCSA Content type text html CSE Assignment Solution Set Friday March Next About this document CSE Assignment Solution Set Friday March Thanks Kromann for the trick this one DPDA for the given language The computation abbac where and the function consists only these rules Here Turing machine that accepts Here brief description all the states Read the first blank This the start the match loop looks for sees then done matching the first string read and looking for the read and looking for the trying match the trying match the Scan left for the Scan left for the next thing match the first string Verify that there other input the second string matched everything the first string and found nothing else the second string accept the input Its computation the input abcab Let Assume all the productions not the form are numbered The PDA will accept the language generated need specify and The function consists the following rules About this document James Fix Tue Mar PST 