Date Mon Dec GMT Server NCSA Content type text html CSE Assignment CSE Assignment Due Friday April class Reading Finish reading Chapter Also read Dalton brief guide CLOS Here the Lisp code for the CLOS demonstration that gave class Friday April Part Sequence Predictor exercise the end Chapter After you get the function NEXT work described the book add one the following additional capabilities recognition sequences that are the results interleaving pairs arithmetic geometric sequences recognition periodic sequences sequences where arbitrary finite sequence repeated over and over recognition sequences that are generated counting modulo increments For example and then the sequence goes Part expression Diagrammer Using the Common Graphics capabilities Allegro Common Lisp for Windows write function DRAW LIST which takes its argument expression which list representing tree and draws the tree new window One way doing this setting array and assigning each node appropriate row and column and then plotting the node labels and connecting nodes and their children with lines see examples the use Common Graphics into Lisp and load some the examples that are there the Allegro subdirectory For extra credit make the tree look nice For example the tree has symmetrical list structure then the plot should look symmetrical too and this should hold true for any subtree well per Jeremy request class Wednesday April please turn your solution the second part this assignment diskette with your name the label tanimoto washington edu 