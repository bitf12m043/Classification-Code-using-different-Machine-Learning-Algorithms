Date Wed Jan GMT Server NCSA Content type text html CSE Assignment Solution Set Next About this document CSE Winter Assignment Solution Set Let and NFAs Construct new NFA that accepts the language Let where for all Behavioral Lemma For all and and only and Suppose regular intersect with any regular language the resulting language also regular For instance regular But and know that this language not regular from class This contradiction must not regular Show that the language not regular Assume regular and let DFA that accepts Suppose contains states Consider the computation any string length more Because there are only total states some state will visited more than once during the computation For example when processes the string the computation looks like this for some and Because visit the same state twice can remove from the string and still end the same state This means that However Also note that since and must greater than This means that falls strictly between and two consecutive perfect squares Therefore not perfect square and This contradiction must not regular About this document James Fix Wed Mar PST 