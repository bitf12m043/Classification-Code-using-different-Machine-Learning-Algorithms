Date Mon Dec GMT Server NCSA Content type text html ZPL Blurbs ZPL Compiler Partial Installation Installation InstructionsZPL Compiler Partial Installation Instructions ZPL Blurb This document describes the requirements and process for setting ZPL compiler partial installation single processor UNIX syste assumed that you are familiar with the ZPL Compilation Process After you have completed the partial installation you may compile ZPL source codes using the Remote ZPL Compiler zcr The partial installation requires less that half hour and fewer than megabytes disk space The resource economy comes from the fact that the ZPL compiler itself not included the partial installation Run time libraries and configuration files are included but order actually compile ZPL program you use client stub connect the ZPL Net Compiler server which compiles the program for you you are not sure you want invest the effort check out the Web Compiler first RequirementsHardwareYou may perform the partial installation any UNIX machine The process which you may perform the partial installation parallel machines will described another document that does not yet exist Software Basic UNIX utilities make tar perl etc ANSI compilerBasicsRecall the two steps the ZPL compilation process First from ZPL source program collection files are generated Then the files are compiled and linked with machine dependent and independent run time libraries produce executable program The partial installation primarily supports the second step The installation includes all the run time libraries and configuration files that select the appropriate compiler compiler flags etc Partial InstallationThis section describes how create partial installation your hardware assumed that you are installing machine that currently supported otherwise you may want consider undertaking the port see the Porting the ZPL System document The installation process involves creating the ZPL project directory hierarchy typically usr local zpl though can anywhere This achieved two steps FTP tar file for and compile and link the run time libraries Download filesFTP the following compressed tar file Sequential UNIX workstation home seq tar Uncompress and untar this file the ZPL project directory usr local zpl zcat home seq tar tar xvf Compile the run time librariesThe first step compiling the run time libraries set the ZPLTARGET environment variable indicate that you want compile for sequential UNIX workstation setenv ZPLTARGET seq actually build the libraries move into the directory where you untarred the file and type the following home src zmake all seq zmake all That You are all set use the remote ZPL compiler See the Using the Remote ZPL Compiler page for introduction zcr ZPL CSE zpl info washington edu 