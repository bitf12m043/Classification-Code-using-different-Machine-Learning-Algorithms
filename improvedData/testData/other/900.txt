Date Tue Dec GMT Server NCSA Content type text html Problem with SkyBlue and CyclesProblem with SkyBlue and Cycles This note alert users SkyBlue problem regarding its handling cycles constraints SkyBlue incremental local propagation constraint solvers for constraint hierarchies SkyBlue more general than the earlier DeltaBlue algorithm that supports multi output methods and allows cycles the constraint graph SkyBlue itself doesn handle cycles but does allow external cycle solvers called satisfy the constraints the cycle The problem that SkyBlue will not always gather enough constraints hand off the cycle solver the constraints the immediate cycle uniquely specify the values for their variables all well However the constraints are redundant incompatible then additional constraints must considered well and SkyBlue won necessarily identify these needed constraints Here trivial example Suppose have the following collection constraints all required The correct solution course However SkyBlue may select just the two constraints and and hand them the cycle solver They are redundant and hence don give unique values for and the cycle solver really needs the other constraints well find the correct solution straightforward solution the problem would simply gather all the constraints downstream from the cycle and pass these the cycle solver However general this will result larger cycles than necessary Another approach would first partition the constraint graph into cyclic and acyclic regions based its topology Next would process all the required constraints all regions communicating variable values between regions they become known then all the constraints the next strongest level and forth This the approach taken the UltraViolet algorithm the approach could also adapted for use with SkyBlue the positive side the basic SkyBlue algorithm the local propagation mechanism including multi output constraints works fine addition Michael Sannella dissertation does describe the problem with gathering constraints for external cycle solver see pages However more recently realized that least for some applications the problem not obscure one that comes only pathological cases but can arise realistic cases your problem domain calls for local propagation only including multi output constraints SkyBlue will serve its purpose well But should modified used with external cycle solvers 