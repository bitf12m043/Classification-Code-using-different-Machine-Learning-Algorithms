Date Thu Nov GMT Server NCSA Content type text html Dynamic Compilation ProjectThe Dynamic Compilation Project More Information Our Dynamic Compiler Project Members Papers Related Projects Only Sections Student Projects Group Web Dynamic compilation enables optimizations based the values invariant data computed run time Using the values these run time constants dynamic compiler can eliminate their memory loads perform constant propagation and folding remove branches they determine and fully unroll loops they bound However the performance benefits the more efficient dynamically compiled code are offset the run time cost the dynamic compile Our approach dynamic compilation strives for both fast dynamic compilation and high quality dynamically compiled code the programmer annotates regions the programs that should compiled dynamically static optimizing compiler automatically produces pre optimized machine code templates using pair dataflow analyses that identify which variables will constant run time and simple dynamic compiler copies the templates patching the computed values the run time constants produce optimized executable code Our work targets general purpose imperative programming languages initially Initial experiments applying dynamic compilation programs have produced speedups ranging from are part the SPIN project Eventually our system will used dynamically compile code the SPIN kernel for example the SPIN event dispatcher However are also actively exploring other possible applications dynamic compilation such virtual machine interpreters Our prototype dynamic compilation system described our PLDI paper are now starting design and build our second generation system will release more details soon Last updated August grant washington edu 