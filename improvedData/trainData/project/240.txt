Date Wed Nov GMT Server Apache SSL Content type text html Content length Last modified Mon Sep GMT The Tower Pizzas The Tower Pizzas ResearchersDr Nick Roussopoulos Principal InvestigatorMichael Tan Graduate StudentStephen Kelley Research AssociateDescription have constructed the Tower Pizzas TOPs multi user striped storage system The main goals TOPs are provide access data striped across workstations exploit caching and prefetching the client and server implement the system high level for portability explore data layout The system implemented software top general UNIX This allows workstations heterogeneous flavors UNIX work together clients and servers Server workstations run single server process and clients talk remote servers through local server process through linked library The local and remote server process are identical which allows TOPS run peer peer collection workstations rather than just partitioned set clients and servers The local and remote server processes provide buffer management striping configurable per file over the network and disk services including asynchronous read write Local clients access buffers the local server through shared memory Asynchronous read write disk provided through the server threads and aio calls File meta data centralized server process accessed only open close TOPs has been implemented over the past year and have now run variety hardware clusters SPARCs and Alpha connected Ethernet and our node using TCP over the high speed switch the can demonstrate linear scalability global throughput servers and clients are added this point are now starting more detailed investigation caching and prefetching strategies are also examining new data placement techniques Performance the three tests below clients are run using servers srvs The axis gives the total system throughput the sum the throughput delivered each client Note that the filesystem and disks used for these tests can transfer data for sequential reads and about for random reads Reading cached server data this test clients read from small file server The file completely cached the server disk other than the initial load from disk incurred The test shows the overhead TOPs and indicates the maximum performance possible this platform Group sequential read with prefetch ordering strategy this test each client read sequentially through large file The portions the file read each client was disjoint from one another used prefetching strategy maintain sequential access the disk the requests from the clients were not synchronized Reading from large disk files these tests each client continually requested small contiguous portion the data file Each request started some random point the file The data file was relatively large per server and could not entirely cached Publications technical report submitted for publication and brief set slides about TOPs available Last updated Fri September 