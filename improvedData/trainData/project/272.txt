Date Tue Nov GMT Server NCSA Content type text html Graph TheoryGraph Theory Primary Investigators Grant Cheston and Mark Keil Algorithmic Development Restricted Classes Graphs Many problems graph theory are complete general graphs practice one rarely has contend with general graphs often information available limit the graphs restricted class particular graphs which arise from geometric models are common Also there are problems that can solved efficiently for some these restricted classes for example maximum clique circle graphs this project employ various techniques including generalized form dynamic programming design polynomial time algorithms for problems various restricted classes graphs Sometimes graph parameters that are not generally equal are equal for certain restricted classes This can useful showing the problems are complete developing algorithms Finally useful study new classes graphs and determine for wide variety problems whether they are complete not Even Adjacency Split Graph The objective partition the vertices graph into two sets and such that each vertex one set adjacent vertex the other set and the two sets are within one having the same size Determining whether graph has Even Adjacency Split EAS complete for general graphs Nevertheless there algorithm for trees and linear time algorithm has been found that handles graphs where vertex adjacent more than two vertices degree Fractional Graph Parameters For some time has been useful describe graphs terms variety parameters for example the chromatic number the clique number the independence number the matching number and the domination number All these parameters can expressed terms integer functions the graphs Recently several people have started investigate fractional variations these parameters where the functions map the real interval rather than the set integers This project has been investigating the Upper Fractional Domination Number GAMMAf have shown that GAMMAf always computable and rational but general hard compute For trees cycles simplicial graphs and trees have shown that GAMMAf GAMMA the corresponding integer problem and this value easy compute Also have shown that for large subclass the class Perfect Graphs the subclass called the Strongly Perfect Graphs four parameters are equal These four parameters are beta GAMMA GAMMAf and where beta the Independence number and the Upper Irredundance number Other classes graphs have also been studied determine whether these parameters are equal but generally they are not equal 