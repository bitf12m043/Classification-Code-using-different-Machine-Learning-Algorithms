Date Tue Nov GMT Server NCSA Content type text html Last modified Thu Sep GMT Content length Glew High Level Editting Glew High Level Editting have had long time interest higher level editting operations both for normal programming languages and for hardware design HDLs the MIT Programmer Apprentice This web page will serve achor for own thoughts the issue particular for record scenarios which this could apply Semantic Split common operation involves spliting variable parameter need edit GNU Emacs dynamic abbreviations package provide completion end line instead end word Unfortunately the package presently has single variable AbbreviationRegexp which using two different contexts dtermine the symbol that you are looking and determine the expansion need split this regexp that can control these capabilities separately Unfortunately there way this without creating complete copy call tree several dozen functions And course create this copy then any changes the original will not propagate copy unless succeed subverting the original implementation reimplement terms the refined split semantics Recursive Parameter Application noticed this possible HLEO High Level Editting Operation while maintaining getnumber package This package relies recursion provide context sensitive number parser that accepts most numeric formats Each recognizer has only limited knowledge syntax but recurses through the top level recognizer handle variants thus obtaining modularity and easy extensibility Initially the calling tree looked bit like DgetnumberDgetnumberlistNumeric formats like decimal terminal further calls recursion Expression formats like Recurses through Dgetnumber Address sets TopLevelRecognizer Dgetnumber Address when calls Dgetnumberlist Dgetnumber sets TopLevelRecognizer Dgetnumber when calls Dgetnumberlist Dgetnumberlist with TopLevelRecognizer Dgetnumber TopLevelRecognizer Numeric formats like decimal terminal further calls recursion TopLevelRecognizer Expression formats like Recurses through TopLevelRecognizer either Dgetnumber depending usage handle the subcomponents the expression after the operator has been recognized Therefore the high level editting operation this calling tree take all instances the explicitly named operation value and make them refer parameter that passed along all paths the calling tree Similarly the reverse operation may occasionally useful interactive query framework would help Well just completed the above edit hand took more than six hours was complicated the fact that semantic split type needed done and incomplete type checking function parameters the compiler was using but this emphasizes the point something that was asa simple describe above took excessively long time because the attention detail that could automated Hidden Recursive Parameters more specific example this arose because wanted pass hidden state that only useful one the lower functions the calling tree not appropriate make this extra info explicit parameter because the information only interest the upper level function that set and the lower level function that receives None the intervening levels functions care about what being passed want transparently communicate something between two different levels the calling hierarchy without affecting minimally affecting the intermediate layers typical naive solution this with global variable but that not safe Again the best solution pass the hidden information parameter through all the intermediate levels But addition creating the parameter also need create the data types that can hide what being passed Furthermore because quite likely that there will need multiple different such instances communication desirable create generic data hiding mechanism call tree message passing mechanism that can handle several such messages transparently All functions such calling tree might have generic parameter CallingTreeMessageList which would composite object list messages with source destination addresses etc The high level routine might something like this call generic call tree placing message specialized subfunction variety while the receiver might need extra info not standard call parameters scan CallingTreeMessageList see there Header glew public html RCS high level editting html glew Exp 