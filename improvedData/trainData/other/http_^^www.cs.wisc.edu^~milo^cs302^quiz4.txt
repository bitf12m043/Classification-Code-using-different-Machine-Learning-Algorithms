Date Mon Nov GMT Server NCSA Content type text html Last modified Wed Nov GMT Content length Quiz Fall Section Fall Section Quiz Take Home Due Monday November This quiz take home assignment that counts the same class quiz Like class quiz this should all your own work though you are welcome use your text and computer assist you this assignment Your answers may written out hand preferably typed points Write function called display that takes two parameters array characters and integer for size the array The function should write the first characters the array cout where the size parameter that was passed into the function Precondition The size passed will not longer than the actual size the array points Write function called num equal that takes three parameters two arrays integers and integer variable for the number valid entries the array most cases just the size the array The function should return the number pairs integers that are the same For example the function was passed two arrays and and size the function would return Precondition The size passed will not longer than the actual size either array points Design and implement QuizScores class This class will used read and average quiz scores All quiz scores are integers from the range You will need the following private variables array integers hold the scores Declare this contain least elements integer keep track how many quiz scores you read You will need the following public methods constructor which takes single ifstream parameter and reads the data from that file The first integer the data file the number quiz scores The rest the file contains integer quiz scores where the first integer the file The data should read from the file and stored the private array variable and also set the integer that keeps track the number scores entered the number scores the file greater than the size the array hold the scores give error message and exit the program average method which takes parameters and returns the average quiz score double getNumber method which returns the number quiz scores read points Modify your QuizScores class adding another public method that will produce histogram the results The method should take single ofstream parameter and write that stream histogram the quiz results this you will need local array variable with elements possible scores Use this array count how many time each score occurs the file For example the quiz scores were the following should displayed Note This question only two points due the extra time will take complete you can not finish this can figure out the most you can lose this problem only two points 