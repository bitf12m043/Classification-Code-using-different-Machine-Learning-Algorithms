MIME Version Server CERN Date Sunday Nov GMT Content Type text html Content Length Last Modified Friday Nov GMT Program ClarificationsCS Program Clarifications November Added November Part Part asks you compare the running time Parts and Unfortunately since sorting only elements both parts execute less than second for Part answer this question Which part should execute faster Why Added November make array make array say that has one argument array Then the function refers These should the same the function should void make array elemptr int for rand Added November Mac Memory Limits Macs have memory limit that you can statically declare only bytes data result have change the number things that you have sort Part Each int bytes should able fit ints However need little room for additional stuff like function calls which take memory only sort integers For the output print every integer per line Only the size the array make array changes Part Each elem bytes could only fit result dynamically allocate the array You don know how this give you the code will sort array elems printing every for output Change make array void make array elem int elem malloc sizeof elem NULL printf Error out memory exit for Also your main should void main void elem make array you add the rest the code Recall the array pointer duality presented class pointer elem However dynamically allocated array for point result the first element that array and the last element your quicksort function you can treat though was array elems Part will sort array pointers elems printing every for output Only the size the array make array changes Added November Partitioning Example There very minor typo the partitioning example page the third line says However since Date Wed Jan GMT Server Apache Content type text html Content length Last modified Sun May GMT Computer Science System InformationComputer Science System InformationCluster Status ReportsSystems Request Tracking System QueueOn Line UNIX and Software HelpHTML tutorials and FAQ sWorld Wide Web Site InformationSystem Staff Information 