Date Sun Nov GMT Server NCSA Content type text html Last modified Wed Jul GMT Content length Shadow Tip Making Shadows Shadows are powerful perceptual cues for the relationship objects scene The renderer does not produce shadows but possible generate acceptable shadows modifying the colors individual vertices objects scene The macro described here performs the following steps The obects shadowed are transformed the coordinate system light places arbitrary direction infinity depth buffer calculated from the viewpoint the light The depth buffer turned into volume data set stacking with itself such way that overlaps all the vertices the original objects The volume depth buffer mapped onto the original vertices new data The depth buffer value each vertex compared the depth the vertex the vertex farther from the light than the value given the depth buffer dimmed The objects with modified colors are transformed back world coordinates This approach sort vertex mapped buffer method has some limitations The accuracy the shadows depends the density vertices object Objects with just few vertices will have jaggie shadows thrown onto them The resolution the depth buffer must high get acceptable shadows The Compute module cannot resolve transform objects Any objects built using Rotate Translate Scale Transform modules will not shadow correctly unless you use the Calculate macro apply the transforms The module uses lot memory runs well machine with Gbyte memory The macro dialog box shown below Explaination inputs The object input carries the objects shadowed against each other The depth resolution input the resolution the desired depth buffer The shadow factor the amount dim shadowed vertex The light pos the direction the light infinity The depth ingore input the threshold for self shadowing Set zero will cause surfaces dim themselves The object scale input overrides the automatic conversion the object size camera coordinate space The offset input allows the objects move along the axis the light download the test program click here You also need configuration file from here The test program layout shown here example output from the program shown below download the macro click here Another example shown below protein ribbon representation casting shadows itself and two planes See also Cornell Theory Center Examples visualization work from CTC final projects spring Cornell Theory Center Last updated November Bruce Land Date Tue Jan GMT Server NCSA Content type text html PoliciesPoliciesYou can discuss homework problems and programming projects with your fellow students general terms but details your writeup and your code should your own work Exams are closed book closed notes and closed neighbor You must not discuss the exams with anyone other than Don Alton before all students have turned their exams pursue cheating and plagiarism the maximum extent permitted the university The Department Computer Science Chair Prof Steve Bruell MacLean Hall Students can contact him arranging meeting with the Computer Science secretary who MacLean Hall the Home Page 